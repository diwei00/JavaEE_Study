server.port=8081

# 配置数据库的连接字符串
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/chatroom?characterEncoding=utf8
spring.datasource.username=root
spring.datasource.password=wuhao1126
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# 设置 Mybatis 的 xml 保存路径
mybatis.mapper-locations=classpath:mapper/*Mapper.xml
# 配置打印 MyBatis 执行的 SQL


mybatis.configuration.log-impl:org.apache.ibatis.logging.stdout.StdOutImpl
# 配置打印 MyBatis 执行的 SQL
logging.level.com.mybatis.demo=debug


# 邮件邮箱
spring.mail.username=2945608334@qq.com
# 邮箱授权码
spring.mail.password=nkomvuzvtxoxdedh
# 配置主机
spring.mail.host=smtp.qq.com
# 配置安全连接
spring.mail.properties.mail.smtp.ssl.enable=true

# 配置session存储在redis中
spring.redis.port=6379
spring.redis.database=0
spring.redis.host=127.0.0.1
spring.session.store-type=redis
# session对象序列化到redis中才会触发数据刷新操作
spring.session.redis.flush-mode=on_save
# session对象过期时间
server.servlet.session.timeout=1800
spring.session.redis.namespace=spring:session

# 配置文件上传大小限制
server.compression.enabled=true
spring.servlet.multipart.max-file-size=2000000
spring.servlet.multipart.max-request-size=2000000
# 配置运行环境
spring.profiles.active=dev
